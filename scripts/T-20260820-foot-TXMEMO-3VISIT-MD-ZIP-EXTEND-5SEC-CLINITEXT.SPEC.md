# 내원 3회 이상 환자 경과분석 ZIP — 섹션5 "임상 유의미 텍스트" 추출 사양서

> 원장님 사전 리뷰용 — 이 사양대로 45명 자료를 뽑습니다. 아래 판단 방식에 이견 있으시면 반영 후 최종 ZIP 확정하겠습니다.
> (예시 문장은 설명용 가상 문장입니다. 실제 환자 정보 아님.)

## 이 자료가 무엇인지

- 대상: 내원 3회 이상 환자 **51명** (취소 제외 내원 기준, 테스트계정 제외).
  ⚠ 앞선 03:31 추출 시점엔 45명 → 8/20 당일 진료(오늘 체크인 87건)로 6명이 3회째를 넘겨 **51명으로 증가**. 코호트 기준(내원 3회+)은 동일, live DB 기준 재생성이라 인원만 늘었습니다.
- 환자 1명당 md 파일 1개, 아래 **5개 묶음**을 한 파일에 순서대로 담습니다.
  - 문서 상단에 **【행정】 성함·차트번호**를 명시합니다(★원장님 요청 반영 — 파일명뿐 아니라 문서 안에도).
  1. 치료메모 (방문일별)
  2. 처방내역
  3. 과거력 (셀프접수 발건강 질문지)
  4. 첫날 상담차트 (초진 기록)
  5. **임상 유의미 텍스트 ← 이번에 새로 추가하는 부분 (아래가 그 설명)**

## 섹션5는 왜 만드나

상담메모·진료차트에는 **매번 반복되는 정형 서식**(예: "성함 :", "보험 : Y/N", "통증:유/무" 같은 빈칸 서식)과, **그 환자에게만 해당하는 특이 내용**(예: "우체국 실비 한도 20만", "6회차부터 ALT 병행")이 섞여 있습니다.

섹션5는 **반복되는 정형 서식·빈칸은 걷어내고, 그 환자 고유의 내용만** 방문 날짜순으로 모아 경과를 한눈에 보게 만드는 묶음입니다.

## 어디서 가져오나 (소스)

| 구분 | 소스 | 2026-08-20 실측 |
|------|------|------|
| 상담메모 (전 방문 전량) | `customer_consult_memos` (삭제분 제외) | 45명 중 43명 보유, 58건 |
| 진료차트 (전 임상필드) | `medical_charts` 임상 6칸 | **45명 전원 디지털 입력 0건** → "기록 없음" 정직 표기 (펜차트로 기록하신 것으로 추정) |
| "루틴상용구" 사전 | `phrase_templates` + `super_phrases` 실제 등록분 | 72건 (상담·차트 상용구) |

## 섹션5 판단 기준 — 무엇을 빼고(a) 무엇을 남기나(b)

원장님 말씀("루틴 상용구는 필요 없다, 그 환자 개별 진료·상담의 특이정보가 필요하다")을 그대로 두 갈래로 정리한 것이 섹션5의 판단 기준입니다.

**(a) 빼는 것 — 루틴 상용구 (모든 환자에게 똑같이 반복되는 것)**
- 정형 서식·템플릿 문구 (등록된 상용구와 글자 그대로 같은 내용)
- 전 환자 공통 반복문 — 표준 주의사항, 동의 상용문, 자동 생성되는 정형구
- 안 채운 빈칸·코드성 필드 (예: `피검사 :`, `통증:유/무` 처럼 값 없이 서식만 남은 줄)
- 빈 줄·구분선·번호만·의사 서명(Dr.○○○)만 있는 줄

**(b) 남기는 것 — 그 환자만의 특이정보 (경과 분석에 실제로 쓰이는 것)**
- 그 환자에게 개별적인 임상 소견
- 경과 변화 (호전·악화·정체 등 방문마다 달라지는 기록)
- 특이 반응·부작용·이슈
- 상담 중 드러난 그 고객만의 개별 맥락 (예: 실비 한도, 병행 시술 안내, 개인 사정 등)

**경계 원칙 — 애매하면 남깁니다(포함 쪽).** 이 자료는 원장님이 직접 골라 쓰시는 원천 데이터이므로, **빠뜨리는 것보다 조금 더 담기는 쪽이 안전**합니다(누락 < 과잉). 걷어낸 줄 수는 항상 표기해 조용히 빠지지 않게 합니다.

## 위 (a)/(b)를 실제로 어떻게 가려내나 — 판단 규칙 5가지

아래 R1~R5가 위 (a)빼기 / (b)남기기를 기계가 실제로 실행하는 방식입니다.

**중요: 모든 판단은 정해진 규칙(기계적)으로만 합니다. 외부 AI(챗봇 등)에 환자 내용을 절대 보내지 않습니다.**

| 규칙 | 내용 | 처리 | (a)/(b) |
|------|------|------|------|
|------|------|------|
| R1 | 메모 전체가 등록된 상용구와 글자 그대로 같으면 | **제외** (내용 없음으로 표기) | (a) |
| R2 | "라벨 : " 형태인데 값이 비었거나 미선택 표시(Y/N, 유/무, -, N/SY/DY/PY, n/a 등)만 있으면 | **제외** (안 채운 빈칸·코드성) | (a) |
| R3 | "라벨 : 실제 값"으로 값이 채워져 있으면 (**"없음"도 실제 값으로 포함** ★) | **포함** (그 환자 정보) | (b) |
| R4 | 라벨 없이 자유롭게 쓴 서술 문장 | **포함** | (b) |
| R5 | 빈 줄 · 구분선 · 번호만 · 의사 서명(Dr.○○○)만 있는 줄 | **제외** | (a) |

- **누락 방지**: 각 메모 아래 "정형·미기재 N줄 제외"라고 몇 줄 걷어냈는지 표기합니다(조용히 빼지 않음).
- 걷어낸 뒤 남는 게 없으면 "유의미 텍스트 없음(정형서식/미기재)"이라고 명시합니다.

### 실제로 이렇게 나옵니다 (가상 예시)

원본 상담메모:
```
성함 : 홍길동
차트번호 : F-0000
피검사 :
통증:유/무
보험 : O / ○○해상 20만
기타 : 실비 한도 부담된다고 하심, 6회차부터 ALT 병행 안내
```

섹션5 결과:
```
- 성함 : 홍길동
- 차트번호 : F-0000
- 보험 : O / ○○해상 20만
- 기타 : 실비 한도 부담된다고 하심, 6회차부터 ALT 병행 안내
  (정형·미기재 2줄 제외)   ← "피검사 :"(빈값), "통증:유/무"(미선택) 걷어냄
```

## 이번 추출 결과 요약 (원장님 답변 반영 재생성 기준 · 2026-08-20)

- 대상 51명 / 상담메모 보유 49명
- 섹션5 유의미 텍스트: **49명 · 총 598줄 발췌 / 102줄(정형·미기재) 제외** (상용구 사전 72건 기준)
- 진료차트(디지털): 전원 기록 없음

## 원장님 확인 답변 반영 결과 (2026-08-20 · 세 가지 판단 포인트 → 확정)

1. **성함/차트번호(행정 항목) = 포함 + 강화.** 문서 상단에 **【행정】 성함·차트번호 헤더**를 별도로 명시했습니다(파일명뿐 아니라 문서 내부 필드로). — 경과분석자료 작성 목적.
2. **"없음"이라고 적힌 값 = 포함.** "없다는 것도 정보"라는 말씀대로, `기저질환 : 없음`·`특이사항 없음` 같은 명시적 "없음"은 **데이터로 포함**합니다. 순수 빈칸·미선택 표시(유/무·n/a 등 코드성)·전 환자 공통 상용구(72건)만 걷어냅니다(누락<과잉).
3. **진료차트 디지털 전원 비어있음 = "기록 없음" 표시(강제 조회 안 함).** 펜차트 사용으로 추정되어 그대로 "기록 없음"으로 표기했습니다. (이미 반영됨)

위 3항 반영해 최종 ZIP 재생성 완료했습니다. 추가 이견 주시면 다시 반영하겠습니다.

---
*추출 스크립트: `scripts/T-20260820-foot-TXMEMO-3VISIT-MD-ZIP-EXTEND-5SEC-CLINITEXT.mjs` (READ-ONLY, DB 변경 0, 외부 AI 0). 산출 ZIP은 요청자 본인(대표원장)에게 동일 스레드로만 전달.*
