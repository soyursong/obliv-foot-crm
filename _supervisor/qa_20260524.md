# Supervisor QA Report — 2026-05-24

## T-20260523-foot-ROOM-DISABLE-TOGGLE — 대시보드 슬롯 방별 비활성화 토글

**판정: GO (Yellow)** | P2 | DB 변경 있음 | Vercel 자동배포 | 독립 재검증 PASS

> ⚠️ GO_WARN (1/5 DB): daily_room_status 신규 테이블. 마이그레이션 적용 완료 확인됨.

### Phase 1: 코드 QA

| 항목 | 결과 | 비고 |
|------|------|------|
| 빌드 | ✅ PASS | `npm run build` 3.37s, exit 0 |
| 기존 기능 영향 | ✅ PASS | RoomSlot 신규 props optional — 기존 경로 미영향 |
| DB 호환성 | ✅ PASS | daily_room_status 신규 테이블. UNIQUE(clinic_id,date,room_name) upsert-safe. 기존 테이블 스키마 변경 없음 |
| 권한/RLS | ✅ PASS | `is_approved_user()` read / `is_admin_or_manager()` write. 함수 `20260426000000_rls_role_separation.sql`에서 확인 |
| 롤백 SQL | ✅ PASS | `20260524010000_daily_room_status.down.sql` — `DROP TABLE IF EXISTS daily_room_status` 확인 |

### Phase 1.5: env 매트릭스

| 항목 | 결과 |
|------|------|
| 신규 env 변수 | ✅ 없음 (fix commit grep 결과 0건) |
| 기존 변수 | ✅ `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` — 운영 bundle 확인 |
| 운영 bundle hash | ✅ `index-BnV8Af6e.js` — 티켓 `bundle_hash` 필드와 일치 |

### Phase 7.5: Runtime Safety Gate

| 패턴 | 위치 | 판정 |
|------|------|------|
| `fetchInactiveRooms` data 접근 | `Dashboard.tsx:2888` — `(data ?? []).map(...)` | ✅ null 가드 정상 |
| `inactiveRooms.has()` | `Dashboard.tsx:2897, 4674` — `useState(new Set())` 초기화 보장 | ✅ 안전 |
| `inactiveRooms?.has()` | `Dashboard.tsx:1053` — optional chaining 추가 적용 | ✅ 안전 |

### Phase 2: E2E (Playwright)

**결과: 6/6 PASS (42.2s)**

| 테스트 | 결과 | 로그 |
|--------|------|------|
| AC-1/AC-2: 방 헤더 토글 버튼 + grayed-out | ✅ PASS | 끄기 클릭 → 비활성 방 1개 확인, 비활성 배지 OK, 복원 OK |
| AC-4: 비활성 방 + 기존 예약 경고 | ✅ PASS | 대시보드 정상 로드, 카드 삭제 없음 |
| AC-3: 당일 한정 (오늘만 토글) | ✅ PASS | 토글 버튼 28개 (오늘 날짜), 날짜 네비 없어 코드 레벨 검증 대체 |
| AC-6: admin/manager 토글 표시 | ✅ PASS | 토글 버튼 28개, 콘솔 에러 0건 |
| 회귀: 대시보드 기본 기능 무결 | ✅ PASS | 루트 컨테이너 OK, 방 슬롯 28개, 크리티컬 에러 0건 |
| auth.setup | ✅ PASS | 로그인 정상 |

### 브라우저 시뮬레이션

- 운영 URL `https://obliv-foot-crm.vercel.app` — 로그인 페이지 정상 렌더 (white screen 아님) ✅
- Vercel 응답: `x-vercel-cache: HIT`, `last-modified: Sat, 23 May 2026 22:43:15 GMT` (commit 이후) ✅
- bundle hash `BnV8Af6e` 운영 HTML 확인 ✅

### 최종 판정

| 판정 | Yellow (GO) |
|------|-------------|
| 배포 | 2026-05-24T04:06:16+09:00 (Vercel 자동배포) |
| commit | 53ea0ebe942c86f2ac3747f57e7f579c12947fe1 |
| bundle_hash | BnV8Af6e |
| field_soak_until | 2026-05-25T04:06:16+09:00 |
| 재검증 시각 | 2026-05-24T08:01 KST |

> 독립 재검증: 티켓 현재 `status: deployed` 확인. 모든 QA 게이트 통과. 이전 supervisor 배포(04:06) 정확.
