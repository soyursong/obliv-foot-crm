-- T-20260620-foot-MUNJIEUN-CLINICMGMT-LOCKOUT — 옵션 A 복구 DML (문지은 대표원장 운영최고권한 flag set)
-- ⚠️ DO NOT APPLY 단독: 반드시 선행 20260619220000_user_profiles_has_ops_authority_additive.sql 적용 후 실행.
--    .DDL_DIFF_HOLD suffix → db push 대상 아님. supervisor DDL-diff 게이트(ADDITIVE+flag set, 대표게이트 면제 autonomy §3.1) 통과 후 수동 실행.
--
-- ════════════════════════════════════════════════════════════════════════
-- ⚠️ STALE-CORRECTION (2026-06-20 02:10 KST): 아래 "옵션 A만이 복구경로/재배포 불필요" 결론은
--    02:00 Vercel STALL 가정下 산출됨. 그 후 Vercel un-stall → 라이브 번들 index-CjZ9qst9.js
--    (last-modified 17:07:56GMT)의 canEditClinicMgmt 가 director escape(옵션 B, b0de7b3a) 포함
--    = `has_ops_authority‖admin‖director`. ⇒ 문지은(director) lock-out 은 옵션 B 라이브로 *이미 복구*.
--    본 DML(옵션 A)은 더 이상 emergency 복구가 아니라 **converge step**(additive 마이그 landing + flag set
--    후 director escape 1줄 제거 → EDIT=has_ops_authority 단독 모델)으로만 의미. supervisor DDL-diff 게이트
--    통과 후 적용. 아래 본문은 history 보존용(stall 시점 추론).
-- ════════════════════════════════════════════════════════════════════════
-- 왜 옵션 A 인가 (dev-foot AC1 번들해시 검증 결론, 2026-06-20 02:00 KST · STALL 가정下, 위 보정 참조):
--   prod 라이브 번들 index-BvHlKg4G.js 의 canEditClinicMgmt 가 has_ops_authority 를 *읽는다*:
--     canEditClinicMgmt(t) = t.has_ops_authority===true ? true : t.role==="admin"
--   ⇒ ROLE-MATRIX has_ops_authority-aware FE 게이트가 이미 prod live (last-modified 23:01 KST 6/19).
--   ⇒ 따라서 has_ops_authority=true 적재만으로 *라이브 번들 그대로* 문지은 EDIT 복구. ★Vercel 재배포 불필요★.
--   ⇒ 옵션 B(director-escape)는 신규 FE 번들(b0de7b3a) 배포 필요하나 Vercel 파이프라인 stall 로 prod 전파 불가
--      (재트리거 0cf6745c 후 8회 폴링 17:03GMT까지 index-BvHlKg4G.js 고정). 즉 현재 옵션 A 만이 Vercel 독립 복구 경로.
--   ⇒ planner 01:55 open-question("구 번들 live면 flag set inert") 해소: 구 번들 아님, has_ops_authority 참조 번들 live → 옵션 A 유효.
--
-- AC2(복구): 문지은 has_ops_authority=true → 라이브 번들 canEditClinicMgmt=true → 진료관리 EDIT 활성.
-- AC3(무회귀): 타 직원 has_ops_authority=false(컬럼 DEFAULT) 유지 → admin escape(admin 11명)만 EDIT, 봉직의/일반직원 read-only 유지.

BEGIN;

-- ════════════════════════════════════════════════════════════════════════
-- ⚠️ CASUALTY-CORRECTION (2026-07-25, STEP3 landing 재개 시 dev-foot 발견) — ★단건 가정 BROKEN★
--   원래 가드: "prod 유일 director(문지은, id prefix d343769a) 뿐 → role='director' = functionally 단건."
--   ★현재 무효★: T-20260724-foot-JUYEON-DOCWRITE-1WK-TEMPACCESS(deployed 2026-07-25, mig 20260725170000)
--     가 김주연 총괄(U0ATDB587PV)을 1주 임시 director 로 승격(baseline=admin, on-request revert).
--     ⇒ 2026-07-25 시점 prod director = 2명(문지은 permanent + 김주연 temp). "단건" 주장은 STALE.
--   ★영향 판정(NON-DESTRUCTIVE·intent-aligned)★:
--     · 문지은(director, permanent) → has_ops_authority=true = 본래 대상. ✓
--     · 김주연(director, temp)      → has_ops_authority=true 로도 세팅됨. 이는 ROLE-MATRIX reassignment_confirm
--       (2026-06-20 문지은 대표원장 U0ALGAAAJAV 확정: '김주연 총괄 = has_ops_authority=true 유지, 401i ops-X
--       downgrade 기각')와 ★정합★ — 김주연은 운영최고권한 보유가 확정 intent. 값 자체는 의도대로.
--     · 타이밍 의존: 김주연이 admin baseline 으로 revert 후 apply 되면 이 UPDATE 는 문지은 1행만.
--       김주연은 admin escape(hasOpsAuthority: role==='admin')로 운영권한 유지 → 어느 순서든 self-healing.
--       (flag=true 가 admin 계정에 남아도 redundant·무해. revert 는 role만 바꾸고 flag 미접촉.)
--   ★결론: 대상셋 = {문지은} ∪ {temp-window 중이면 김주연} = 1~2행. 둘 다 has_ops_authority=true 가
--     confirmed intent 와 정합하므로 role='director' blanket 유지가 안전(silent downgrade 無, 확대 無).
--     ▷ 단 실 대상셋·행수는 supervisor DDL-diff 시점 prod 실측으로 freeze·검증(Data-Correction SOP:
--       single-count 가정 금지, 판정근거 스냅샷 동봉). 김주연 flag 지속 여부에 대한 별도 planner 확정이
--       필요하면 그 판정을 우선한다(본 파일은 confirmed intent 기준 기본 경로).
--   id 전체 UUID 는 PHI 마스킹 — 실행자(supervisor)가 prod 에서 확정.
-- ════════════════════════════════════════════════════════════════════════
UPDATE public.user_profiles
   SET has_ops_authority = true
 WHERE role = 'director'
   AND has_ops_authority = false;   -- idempotent: 이미 true 면 no-op

-- 적용 후 검증 (1~2행: 문지은 permanent + [temp-window면 김주연], 전부 has_ops_authority=true 여야):
--   SELECT id, role, has_ops_authority, updated_at FROM public.user_profiles WHERE role='director';
--   ★기대 행수는 apply 시점 prod director 수(1 또는 2)와 일치해야 함 — 불일치 시 abort·재확인.

COMMIT;

-- ════════════════════════════════════════════════════════════════════════
-- ROLLBACK (복구 되돌릴 때만)
-- ════════════════════════════════════════════════════════════════════════
-- BEGIN;
--   UPDATE public.user_profiles SET has_ops_authority = false WHERE role='director' AND has_ops_authority = true;
-- COMMIT;
