# T-20260729-foot-REDPAY-NON2XX-ALERT-ROOTCAUSE — Part A 원인 규명 (진단, read-only)

**요청(최필경 총괄)**: 7/28 16:43 레드페이 웹훅 non-2xx 응답이 (A) **401 서명불일치**(구조적·재발) 인지
(B) **500 DB 순간장애/제약위반**(일회성) 인지 — 응답코드 이력을 로그로 조회해 근거와 함께 판정.
**성격**: `ef_only` 진단 (화면 변경 없음). 추정 금지 — 로그·DB 실측 증거 기반.
**probe**: `scripts/T-20260729-foot-REDPAY-NON2XX-ALERT-ROOTCAUSE_partA_probe.mjs` (READ-ONLY, write/DDL 0)

---

## 판정: ✅ **(B) 일시장애 — 정확히는 503 (Service Unavailable, 플랫폼 게이트웨이 순간 이용불가).** 401 서명불일치 **아님**.

### 근거 1 — EF 응답코드 실측 (function_edge_logs, Management analytics)

7/28 16:24~16:55 KST redpay-webhook 응답코드 이력(실측):

| KST 시각 | status | method | 비고 |
|---|---|---|---|
| 2026-07-28 16:43:08 | **200** | POST | trxid …15160 (₩8,800) 적재 |
| 2026-07-28 16:43:16 | **503** | POST | ★ 총괄님이 감사 중 발견한 non-2xx |
| 2026-07-28 16:44:20 | **200** | POST | trxid …99427 (₩42,000) 재시도 적재 |

- **문제의 16:43 응답코드 = 503** (401 도 500 도 아님).
- 503 = Service Unavailable = **레드페이 요청이 우리 핸들러 코드에 도달하기 전/중 Edge 플랫폼(게이트웨이·워커) 순간 이용불가**. (우리 코드는 200/400/401/500만 반환 — 503은 우리가 만든 코드가 아니라 플랫폼 레벨 응답.)
- 503은 **재시도 가능(retryable)** → 레드페이 재시도로 즉시 복구됨(직후 16:44:20 = 200).

### 근거 2 — 401 발생 전무 (전 보존기간 스캔, AC-A2 재발범위)

7/28~8/1 redpay-webhook **누계 211건** 응답코드 분포:

| 일자 | 총건 | 분포 |
|---|---|---|
| 7/28 | 53 | 200×45, 405×7, **503×1** |
| 7/29 | 37 | 200×37 |
| 7/30 | 43 | 200×43 |
| 7/31 | 35 | 200×35 |
| 8/1  | 43 | 200×43 |

- **401(서명불일치) 발생 = 0건.** → secret 값·raw body 서명검증 처리의 **구조적 문제 없음**(재발 위험 부재).
- non-2xx 8건 = **503×1**(16:43 그 건) + **405×7**(method_not_allowed = 비-POST 프로브/헬스체크 노이즈, 실제 결제 push 아님).
- 즉, 실제 결제 push(POST) 중 non-2xx는 **오직 16:43 그 503 단 1건**. 이후 재발 0.

### 근거 3 — DB 내구성 증거 (received_at, 401 가설 반증)

`redpay_raw_transactions` 실측 (item2 TRXID2-CAPTURE-VERIFY 교차):

| trxid | approved_at(KST) | received_at(웹훅수신, KST) | 지연 | 적재경로 |
|---|---|---|---|---|
| K1047535845…15160 | 16:40:18 | **16:43:08** | 2.8분 | 웹훅경로(received_at 有) |
| K1047535845…99427 | 16:40:43 | **16:44:20** | 3.6분 | 웹훅경로(received_at 有) |

- `received_at` 은 **웹훅 경로 전용 컬럼**(폴러 경로는 NULL). 값이 있음 = **웹훅 경로로 최종 200 적재됨**.
- **401 가설의 결정적 반증**: 401(HMAC 서명실패)은 deterministic — 동일 secret+동일 raw body면 재시도마다 동일 실패 → 웹훅 경로로는 **절대 적재 불가(영구유실)**. 그런데 2건 모두 웹훅 경로로 적재됨 → 401 가설 falsify.
- approved→received 지연(2.8/3.6분)이 레드페이 **재시도 사다리(1/5/30분)** 와 정합 → 503 후 재시도로 복구된 전형적 패턴. (401이면 "재시도 말라" 신호라 지연-후-성공이 나올 수 없음.)

---

## 결론

- **어느 쪽인가?** → **(B) 일시장애.** 정확히는 **503 Service Unavailable**(플랫폼 게이트웨이 순간 이용불가), 401 서명불일치 아님.
- **재발하는가?** → **아니오.** 전 보존기간 211건 중 401=0, 503=1(16:43 단발). 구조적 secret/raw-body 문제 없음.
- **결제 유실됐는가?** → **아니오.** 레드페이 재시도로 16:43:08·16:44:20 정상 200 적재(item2 확인, 금액·승인번호 일치). 순소실 0.
- **성격**: 일회성 Edge 플랫폼 순간장애 → 재시도 자가복구. 별건 재발방지 승격 **불요**(401이었다면 필요했으나 아님).

## Part B 로 이어지는 함의 (중요 — 정직한 커버리지 고지)

- 이번 건이 우연히 감사로만 발견된 근본 이유 = **non-2xx 응답의 내구성 기록/상시감시 부재**. Part B(상시 슬랙 알림)로 격상.
- **커버리지 한계(no silent cap)**: Part B 는 **우리 핸들러 코드가 반환하는 non-2xx**(401 invalid_signature / 400 body_read_failed / 500 clinic_resolve_failed·db_upsert_failed·unexpected_error)를 즉시 알림. 
  단, 이번 16:43 같은 **플랫폼 레벨 503**(우리 코드 도달 전 게이트웨이·워커 실패)은 우리 JS가 실행되지 않으므로 in-code 알림으로는 **잡히지 않음**. 
  → 플랫폼 503/게이트웨이 non-2xx 상시감시는 별도 **로그기반 모니터**(본 probe의 analytics 조회를 주기화)가 필요 — Part B 범위 밖 후속 P2 권고.
  → 다만 총괄님이 가장 우려한 **구조적 신호(401 서명불일치)** 와 우리 코드의 DB 장애(500)는 Part B 가 100% 커버.
